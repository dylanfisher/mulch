# Secrets
.env
.env.*
!.env.example
*.pem
*.key

# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# This machine's own profiler numbers. Never committed: a run is a photograph of one
# machine, and a committed one becomes a golden someone diffs against across machines.
.profile-history.jsonl

# Caches
.cache/
coverage/
*.log
.vite/

# Editors / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Agent local overrides — the shared config is committed
.claude/settings.local.json

tmp/
