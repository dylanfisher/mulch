/**
 * @role The words the loop's own controls say: what the count that sets its length is called.
 *   Beside src/lib/copy.ts rather than in it because that file is at the hard cap (0045, the
 *   reason src/lib/copyCard.ts and src/lib/copyStrip.ts are where they are).
 * @instead The sentence each of the loop's controls says after a rest, including the snap
 *   toggle's → `ACTION_TOOLTIPS` in src/lib/copy.ts. The one-word labels on the toggle beside
 *   this field, which are written where it is drawn → src/ui/Waveform.tsx. What a beat is →
 *   src/lib/analysis.ts.
 */

/** The caption on the field that sets the loop's length, in the unit it is counted in (0059). */
export const LOOP_BEATS_LABEL = "Beats";
